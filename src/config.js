const config = {
 
  
    apiUrl: 'https://pixel-server-theta.vercel.app/', // API base URL
    uploadUrl: 'https://pixel-server-theta.vercel.app/' // Uploads folder URL

    // apiUrl: 'http://localhost:5000/', // API base URL
    // uploadUrl: 'http://localhost:5000/' // Uploads folder URL
  };
  
  export default config;
  